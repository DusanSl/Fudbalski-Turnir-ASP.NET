@model FudbalskiTurnir.ViewModels.SponzorViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Sponzor</h4>

    @if (ViewBag.IsAdmin)
    {
        <h5 class="text-muted mt-3 mb-2">System Info</h5>
        <dl class="row mb-3">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.SponzorID)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.SponzorID)
            </dd>

            <dt class="col-sm-3">
                TurnirID
            </dt>
            <dd class="col-sm-9">
                @if (Model.Turniri != null && Model.Turniri.Any())
                {
                    @string.Join(", ", Model.Turniri.Select(t => t.TurnirID))
                }
                else
                {
                    <span class="text-muted">Nema povezanih turnira.</span>
                }
            </dd>
        </dl>
    }

    <h5 class="text-muted mt-3 mb-2">Company Info</h5>
    <dl class="row mb-3">
        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.ImeSponzora)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.ImeSponzora)
        </dd>

        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.KontaktSponzora)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.KontaktSponzora)
        </dd>
    </dl>

    <h5 class="text-muted mt-3 mb-2">Sponsorship Details</h5>
    <dl class="row mb-3">
        <dt class="col-sm-3">
            Turnir
        </dt>
        <dd class="col-sm-9">
            @if (Model.Turniri != null && Model.Turniri.Any())
            {
                @string.Join(", ", Model.Turniri.Select(t => t.NazivTurnira))
            }
            else
            {
                <span class="text-muted">Nema povezanih turnira.</span>
            }
        </dd>

        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.VrednostSponzora)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.VrednostSponzora)
        </dd>
    </dl>
</div>

<div class="mt-4">
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    @if (ViewBag.IsAdmin)
    {
        <a asp-controller="Admin" asp-action="Index" class="btn btn-dark">Back to Admin Page</a>
    }
</div>